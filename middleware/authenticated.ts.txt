export default defineNuxtRouteMiddleware(to => {
  const user = useUserStore();

  if (process?.server) {
    return;
  }

  console.log(user.user);
  console.log(user.loggedIn);
  if (!user.loggedIn && to.name !== 'index') {
    // return navigateTo('/', { redirectCode: 301 });
  }
});
