
import type { ConfigInterface } from '~/lib/types/general.types';

const config: ConfigInterface = {
  APP_URL: 'https://api-staging-game.fuel.network',
  GAME_URL: 'https://staging-game.fuel.network',
  API_BASE: 'https://api-staging-game.fuel.network',
};

export default config;
